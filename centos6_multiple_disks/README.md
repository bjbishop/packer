These are [packer](https://packer.io) build scripts to create a Centos
6.8 vagrant box with attached disks.

Execute the `create_disks.sh` command before running `packer build
centos6.json`

Tested with:

- Packer 1.1.0
- macOS Sierra
- VirtualBox 5.1.8
